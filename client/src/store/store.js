import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  strict: true,
  state: {
  },
  getters: {
    /**
     * Renvoie l'utilisateur actuellement connecté
     * @param state
     */
  },
  mutations: {
  },
  actions: {
  }

})
